# Orchestration log

The embodied lab's vantage on the global landscape. Written for coordination
across parallel chats and across ephemeral sessions, so the next agent does not
start cold.

## 2026-06-17 — global awareness refresh

All 14 repos sit on `claude/embodied-ai-somatic-map-vfet1g`. Only `alchemy`
holds this lab's work (12 commits ahead). The other 13 carry recent work from
parallel chats. The map, by cluster:

**Knowledge core (the SSOT and its vault)**
- `stack-data` — single source of truth. New: "The Third Kind of Information"
  book project, sqlite + Formbricks MCP servers.
- `second-brain-mirror` — upstream private Obsidian vault, daily snapshots,
  canonicals including the Say Why facilitation guide.
- `second-brain-v2` — public narrative skin (the Apparatus, The Mirror).
- `three-type-evaluation` — methodology paper. Type 3 reflexive information is
  "embodied attunement," the namesake of the stack-data book project.
- `gene-keys-data` — 64 Gene Keys dataset, standalone.

**Brand and public presence (two identities, one system)**
- `isaacrubinstein.com` — Brand A, Ink/Ochre/Teal, evaluation practice. Launch
  gated on headshot, CV, Lighthouse.
- `rubinsteinproductions` — Brand B, Digital Liver canon (Marrow, Oxblood, Bile,
  Bone, a metabolic palette). Embeds the Information Metabolism Diagnostic.
- `rubinstein-productions-toolkit` — public ops stack, methodology, CLI tools.
- `rp-intranet` — private ops hub, pricing, clients, financials.

**Products and tools (the Alchemy family and friends)**
- `alchemy` — this lab. The embodied-AI somatic map plus the live SEL-1 service.
- `alchemy-diagnostic` — Mode 2 diagnostic, absorbed into Alchemy's Diag view.
  Shipped Tier 1 revenue features (name field, email gate, booking CTA). Now
  needs version parity with the absorbed view.
- `royal-metrics` — business and client ROI PWA. The integration hub: reads
  stack-data, and carries a somatic ease slider.
- `saywhy-app` — Say Why Reflection PWA, parked, shares the rp-api worker.
- `resolve-workflow-v2` — DaVinci Resolve assistant, outlier, Claude vision
  daemon.

## The connective thread

The metabolic and somatic frame is not niche to this lab. It already runs
through the brand and the products: Brand B is literally a metabolic palette,
the Information Metabolism Diagnostic lives in two repos and the marketing site,
royal-metrics put a somatic ease slider on its dashboard, and three-type's Type
3 is "embodied attunement." The embodied-AI wager is the deep engine under all
of it. If the wager pays off, it is core IP for RP's metabolic positioning. If
it does not, that frame still stands on its own; the AI claim is what falls.

## Routed next steps

| # | Step | Owner | Status |
|---|------|-------|--------|
| 1 | Run the wager: Justin accepts, deploy with key, load real reflections | Isaac + Justin | **blocked on Isaac**, holding per approved recommendation |
| 2 | Diagnostic parity: reconcile Alchemy's Diag view with alchemy-diagnostic's Tier 1 features | this lab | ready, in lane |
| 3 | Dress the embodied-service UI and the deck in the Brand B Digital Liver canon | this lab | ready, in lane |
| 4 | Canonicalize the `stack-data.js` client from royal-metrics, reuse in alchemy-diagnostic and saywhy | cross-repo | needs routing |
| 5 | Consolidate the "Third Kind of Information" thread (stack-data book, three-type paper, mirror canonicals) to one source | cross-repo | needs routing |

## Recommendation

The wager is blocked on Isaac, so the best in-lane use of this chat is steps 2
and 3: make the live service and the diagnostic coherent and on-brand before the
wager runs, so a yes from Justin meets a finished surface, not a rough one.
Steps 4 and 5 are cross-repo and want either Isaac's routing or another chat
that owns those repos. Do not start a second user or any SEL-2 work until the
wager returns.
